# Development only — the app has no build dependencies at runtime.
node_modules/
package-lock.json

# Build output. Regenerated by `node build.mjs`; GitHub Actions builds its own.
dist/

# Screenshots delivered during development; the originals live in test/.
Claude outputs/

# Editor and OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
